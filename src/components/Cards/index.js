import React from "react";

const Card = (props) => {
    return (
        <img className="rounded-circle" type="button" 

        style={{width: "200px", height: "200px"}}/>
    );
}

export default Card;